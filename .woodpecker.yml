pipeline:
    build:
        image: python
        commands:
            #- pip3 install nose cython
            - apt update
            - apt install -y cython3 python3-numpy
            - bash install.sh 
